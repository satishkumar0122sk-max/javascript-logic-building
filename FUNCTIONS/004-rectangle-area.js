// Q004 — Calculate Rectangle Area

let length = +prompt("Enter your Length");
let width = +prompt("Enter your width");

function rectangleArea(length,width){
  return length * width;
}
console.log(rectangleArea(length,width));
